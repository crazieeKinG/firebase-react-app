import LoginForm from "../../components/LoginAndLogout/LoginForm";
import { LOGIN_FORM } from "../../constants/constants";

const Login = () => {
    return <LoginForm formType={LOGIN_FORM} />;
};

export default Login;
